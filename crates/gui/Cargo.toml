[package]
name = "hakutaku-gui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Developer GUI for packing and inspecting Hakutaku game packages"

[[bin]]
name = "hakutaku-gui"
path = "src/main.rs"

[dependencies]
hakutaku-core.workspace = true
hakutaku-pack.workspace = true
eframe = { version = "0.31.1", default-features = false, features = ["default_fonts", "glow", "x11", "wayland"] }
egui = "0.31.1"

[target.'cfg(not(target_os = "linux"))'.dependencies]
rfd = { version = "0.15.4", default-features = false }

[target.'cfg(target_os = "linux")'.dependencies]
# GTK is confined to the Linux developer GUI. Core, CLI, macOS, and Windows do
# not inherit a portal async runtime or Linux UI dependency surface.
rfd = { version = "0.15.4", default-features = false, features = ["gtk3"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
